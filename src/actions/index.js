export const loginUser= (user)=>({
  name:user.name,
  email:user.email,
  password:user.password
})

export const logoutUser= (user)=>({
  name:user.name,
  email:user.email,
  password:user.password
})